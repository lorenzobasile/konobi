
plugins {
    id 'java'
    id 'application'
}

task(runConsole, type: JavaExec) {
    main = 'konobi.ConsoleVersion.GameRunner'
    classpath = sourceSets.main.runtimeClasspath
    standardInput = System.in
    standardOutput = System.out
}
task(runClientServer, type: JavaExec) {
    main = 'konobi.ClientServerVersion.GameRunnerCS'
    classpath = sourceSets.main.runtimeClasspath
}

mainClassName='konobi.ConsoleVersion.GameRunner'

repositories {
    mavenCentral()
}

jar {
    manifest {
        attributes (
                'Main-Class': 'konobi.ConsoleVersion.GameRunner'
        )
    }
}


dependencies {
    implementation 'org.junit.jupiter:junit-jupiter:5.4.2'
    testImplementation(platform('org.junit:junit-bom:5.6.2'))
    testImplementation('org.junit.jupiter:junit-jupiter')
}

test {
    useJUnitPlatform()
}
